@using Microsoft.AspNetCore.Http.Extensions
@using Microsoft.AspNetCore.Localization
@model P2FixAnAppDotNetCode.Models.Cart

<div class="">
    @if (Model.Lines.Count() > 0)
    {
        <small class="navbar-text">
            <b>@Localizer["YourCart"]:</b>
            @* Début MODIFICATION *@
            @{
                int totalItems = Model.Lines.Sum(x => x.Quantity);
                var label = Localizer["Item"];

                if (totalItems > 1)
                {
                    label = Localizer["Items"];
                }
            }
            @* Afficher le nombre total d'articles avec le libellé approprié *@
            @totalItems @label
            @* @Model.Lines.Sum(x => x.Quantity) item(s) *@
            @* Fin MODIFICATION *@
            @Model.GetTotalValue().ToString("c")
        </small>
    }
    <a class="btn btn-sm btn-secondary navbar-btn" asp-controller="Cart" asp-action="Index">
        <i class="fa fa-shopping-cart"></i>
    </a>
</div>
